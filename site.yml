- hosts: servers
  sudo: yes
  roles:
    - { role: hosts, tags: hosts }
    - { role: ssh, tags: ssh }
    - { role: selinux, tags: selinux }

- hosts: send-servers
  sudo: yes
  roles:
    - { role: mail, tags: mail }
